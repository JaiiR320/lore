{
  "name": "lore",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "link": "bun link --cwd packages/cli && bun link --cwd packages/mcp",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
