{
  "name": "lore",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "link": "bun link --cwd packages/cli && bun link --cwd packages/mcp"
  },
  "devDependencies": {
    "@lore/core": "workspace:*",
    "@types/bun": "latest"
  }
}
